{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "pnpm --filter=vue-3-slick-carousel run build",
    "dev": "pnpm --filter=nuxt-app run dev"
  },
  "devDependencies": {}
}
